<UserControl x:Class="ChurchBooks.App.Views.SetupWorkspaceView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
 <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
  <StackPanel Margin="14">
   <Grid Margin="0,0,0,12"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
    <StackPanel><TextBlock Text="Organization &amp; Terminology" FontSize="22" FontWeight="SemiBold"/><TextBlock Text="Personalize ChurchBooks with your church's identity and familiar language. Stable internal accounting keys keep reports and history reliable." Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,5,0,0" TextWrapping="Wrap"/></StackPanel>
    <Button Grid.Column="1" Command="{Binding CompleteSetupCommand}" Content="Complete Setup" Margin="12,0,0,0"><Button.Style><Style TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding ShowCompleteSetupAction}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></Button.Style></Button>
   </Grid>
   <Grid Margin="0,0,0,12"><Grid.ColumnDefinitions><ColumnDefinition Width="1*"/><ColumnDefinition Width="12"/><ColumnDefinition Width="1.08*"/></Grid.ColumnDefinitions>
    <Border Style="{StaticResource CardStyle}" Padding="16"><StackPanel><TextBlock Text="Organization Profile" FontSize="16" FontWeight="SemiBold"/><TextBlock Text="Identity used throughout the local books." Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,4,0,12"/>
      <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="130"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><Grid.RowDefinitions><RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/></Grid.RowDefinitions>
       <TextBlock Text="Display name" VerticalAlignment="Center"/><TextBox Grid.Column="1" Text="{Binding OrganizationDisplayName, UpdateSourceTrigger=PropertyChanged}"/>
       <TextBlock Grid.Row="1" Text="Legal name" Margin="0,10,0,0" VerticalAlignment="Center"/><TextBox Grid.Row="1" Grid.Column="1" Margin="0,8,0,0" Text="{Binding LegalName, UpdateSourceTrigger=PropertyChanged}"/>
       <TextBlock Grid.Row="2" Text="Base currency" Margin="0,10,0,0" VerticalAlignment="Center"/><ComboBox Grid.Row="2" Grid.Column="1" Margin="0,8,0,0" ItemsSource="{Binding CurrencyCodes}" SelectedItem="{Binding BaseCurrency, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" ToolTip="Defaults from the selected country; you can override it before saving."/>
       <TextBlock Grid.Row="3" Text="Fiscal start month" Margin="0,10,0,0" VerticalAlignment="Center"/><ComboBox Grid.Row="3" Grid.Column="1" Margin="0,8,0,0" ItemsSource="{Binding FiscalMonths}" SelectedItem="{Binding FiscalYearStartMonth}"/>
       <TextBlock Grid.Row="4" Text="Country" Margin="0,10,0,0" VerticalAlignment="Center"/><ComboBox Grid.Row="4" Grid.Column="1" Margin="0,8,0,0" ItemsSource="{Binding CountryOptions}" DisplayMemberPath="DisplayName" SelectedValuePath="CountryCode" SelectedValue="{Binding CountryCode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsTextSearchEnabled="True" TextSearch.TextPath="CountryName" ToolTip="Selecting a country automatically chooses its standard base currency."/>
       <TextBlock Grid.Row="5" Text="Tax ID / TIN" Margin="0,10,0,0" VerticalAlignment="Center"/><TextBox Grid.Row="5" Grid.Column="1" Margin="0,8,0,0" Text="{Binding TaxIdentifier}"/>
      </Grid>
      <Button Command="{Binding SaveSetupCommand}" Style="{StaticResource SecondaryButtonStyle}" Content="Save Profile" HorizontalAlignment="Left" Margin="0,14,0,0"/>
    </StackPanel></Border>
    <Border Grid.Column="2" Style="{StaticResource CardStyle}" Padding="16"><StackPanel><TextBlock Text="Terminology" FontSize="16" FontWeight="SemiBold"/><TextBlock Text="Example: Member → Partner, Service → Gathering, Offering → Contribution. Visible labels can change without changing accounting history." Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,5,0,10"/><DataGrid ItemsSource="{Binding Terms}" AutoGenerateColumns="False" CanUserAddRows="False" Height="290"><DataGrid.Columns><DataGridTextColumn Header="System term" Binding="{Binding DisplayKey}" IsReadOnly="True" Width="*"/><DataGridTextColumn Header="Singular label" Binding="{Binding Singular, UpdateSourceTrigger=PropertyChanged}" Width="*"/><DataGridTextColumn Header="Plural label" Binding="{Binding Plural, UpdateSourceTrigger=PropertyChanged}" Width="*"/></DataGrid.Columns></DataGrid><Button Command="{Binding SaveSetupCommand}" Style="{StaticResource SecondaryButtonStyle}" Content="Save Terms" HorizontalAlignment="Left" Margin="0,10,0,0"/></StackPanel></Border>
   </Grid>
   <Border Style="{StaticResource CardStyle}" Padding="16"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="1*"/><ColumnDefinition Width="12"/><ColumnDefinition Width="1*"/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Additional Labels / Search Terms" FontSize="16" FontWeight="SemiBold"/><TextBlock Text="Add local words your team already uses so global search can recognize them without changing accounting logic." Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,4,0,10"/><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="140"/><ColumnDefinition Width="8"/><ColumnDefinition Width="*"/><ColumnDefinition Width="8"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><ComboBox ItemsSource="{Binding AliasAreas}" SelectedItem="{Binding AliasArea}"/><TextBox Grid.Column="2" Text="{Binding AliasText, UpdateSourceTrigger=PropertyChanged}" ToolTip="Local search term"/><Button Grid.Column="4" Command="{Binding AddAliasCommand}" Style="{StaticResource PrimaryButtonStyle}" Content="Add Term"/></Grid></StackPanel><StackPanel Grid.Column="2"><DataGrid Height="140" ItemsSource="{Binding CustomAliases}" SelectedItem="{Binding SelectedAlias}" AutoGenerateColumns="False" IsReadOnly="True"><DataGrid.Columns><DataGridTextColumn Header="Area" Binding="{Binding AreaKey}" Width="120"/><DataGridTextColumn Header="Term" Binding="{Binding AliasText}" Width="*"/></DataGrid.Columns></DataGrid><Button Command="{Binding RemoveAliasCommand}" Style="{StaticResource SecondaryButtonStyle}" Content="Remove Selected" HorizontalAlignment="Right" Margin="0,8,0,0"/></StackPanel></Grid></Border>
   <Border Background="{DynamicResource AccentSoftBrush}" CornerRadius="8" Padding="10" Margin="0,10,0,0"><TextBlock Text="{Binding StatusMessage}" TextWrapping="Wrap"/></Border>
  </StackPanel>
 </ScrollViewer>
</UserControl>
